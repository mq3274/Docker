
📌 Docker Commands

Basic Info
- docker version — Show Docker version info
- docker info — System-wide info about Docker
- docker help — Display help for commands

Images & Containers
- docker pull <image> — Download image from Docker Hub
- docker run <image> — Create & start container
- docker run -d <image> — Run in background (detached)
- docker run -it <image> — Run interactively with terminal
- docker exec -it <container> <cmd> — Run command inside running container
- docker start <container> — Start stopped container
- docker stop <container> — Stop running container
- docker restart <container> — Restart container
- docker kill <container> — Forcefully stop container
- docker ps — List running containers
- docker ps -a — List all containers (including stopped)
- docker rm <container> — Remove container
- docker rmi <image> — Remove image
- docker images — List all images

Image & Container Management
- docker build -t <tag> . — Build image from Dockerfile
- docker tag <image> <repo>:<tag> — Tag image for repo
- docker push <repo>:<tag> — Push image to registry
- docker logs <container> — View logs of container
- docker inspect <name/id> — Low-level info of container/image
- docker rename <old> <new> — Rename a container
- docker cp <src> <dest> — Copy files between host & container
- docker commit <container> <new_image> — Create image from container changes

Networks
- docker network ls — List networks
- docker network create <name> — Create network
- docker network inspect <name> — Network details
- docker network rm <name> — Remove network

Volumes
- docker volume ls — List volumes
- docker volume create <name> — Create volume
- docker volume inspect <name> — Volume info
- docker volume rm <name> — Remove volume

Cleanup
- docker system prune — Remove all unused data
- docker image prune — Remove unused images
- docker container prune — Remove stopped containers

Monitoring
- docker stats — Real-time usage stats
- docker top <container> — Show container processes

Docker Registry
- docker login — Log in to Docker registry
- docker logout — Log out of registry
- docker search <term> — Search Docker Hub

Docker Compose
- docker-compose up — Start services via Compose
- docker-compose down — Stop and remove services
- docker-compose build — Build services via Compose
- docker-compose ps — Show status of services
